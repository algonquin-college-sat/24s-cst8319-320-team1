<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- Header Text -->
    <TextView
        android:id="@+id/welcome_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome!"
        android:layout_marginBottom="16dp"
        android:textSize="18sp"
        android:textStyle="bold"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"/>


    <!-- Button for Function 1 -->
    <Button
        android:id="@+id/calender_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/welcome_message"
        android:layout_marginTop="40dp"
        android:text="Calender"
        android:background="#FFC107" />

    <!-- Button for Function 2 -->
    <Button
        android:id="@+id/accountinfo_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/calender_btn"
        android:layout_marginTop="20dp"
        android:text="Account Profile"
        android:background="#FFC107" />

    <!-- Button for Function 3 -->
    <Button
        android:id="@+id/bookAppoint_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/accountinfo_btn"
        android:layout_marginTop="20dp"
        android:text="Booking Appointment"
        android:background="#FFC107" />

    <!-- Button for Function 4 -->
    <Button
        android:id="@+id/babyImage_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/bookAppoint_btn"
        android:layout_marginTop="20dp"
        android:text="Baby Illustration Images"
        android:background="#FFC107" />

    <!-- Button for Function 5 -->
    <Button
        android:id="@+id/reviewDoc_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/babyImage_btn"
        android:layout_marginTop="20dp"
        android:text="Review Documents"
        android:background="#FFC107" />

    <!-- Button to Return to Login Page -->
    <Button
        android:id="@+id/logout_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/reviewDoc_btn"
        android:layout_marginTop="20dp"
        android:text="Back to Login"
        android:background="#D3D3D3" />
</RelativeLayout>